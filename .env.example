# Used only by Docker Compose. Choose a long, random value locally.
WORKER_API_TOKEN=replace-with-a-long-random-token

# Keep empty to run only the local backend/frontend services.
GPU_API_URL=
GPU_WORKER_TOKEN=
GPU_MAX_ARTIFACT_BYTES=2147483648
